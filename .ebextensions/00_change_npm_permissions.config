files:
  "/.npmrc":
    mode: "000744"
    owner: ec2-user
    group: ec2-user
    content: |
      #Force npm to run node-gyp also as root, preventing permission denied errors in AWS with npm@5 or @6
      unsafe-perm=true
